# local build artifacts — release binaries come from CI, never the repo
gateway/delegent
protocol/delegent-proto
dist/
*.img
cacert.pem
inspector/mcp-inspector
inspector/.cache/
gateway/cmd/delegent/delegent
